{
  "name": "pw-framework",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test --grep \"C01\" --headed",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "setup": "playwright test tests/auth.setup.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^25.8.0",
    "dotenv": "^16.4.0",
    "typescript": "^5.4.0"
  }
}
